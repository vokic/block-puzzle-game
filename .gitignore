# Nothing ignored.
# config.js holds the Supabase *publishable* key, which is designed to be public
# (Row-Level-Security protects the data), so it IS committed — this keeps the
# leaderboard working on every deploy (Netlify git deploy, GitHub Pages, etc.).
# NEVER put the *secret* key here.
